spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    aws:
      stack:
        auto: false
      region:
        static: us-east-2

aws:
  s3:
    bucket-name: logistics-packages-test
    auto-create-bucket: true